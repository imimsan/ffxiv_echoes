# 実装ロードマップ

絶妖星乱舞（2026年6月2日実装予定）に向けた段階的な実装計画。

---

## 全体方針

- **MVPとフェーズ2を絶妖星乱舞実装日までに完成**を目標
- **フェーズ3は絶実装後の攻略中に追加実装**
- 各フェーズは独立してリリース可能（前のフェーズが動作した状態で次に進む）
- Claude Code を活用した開発を前提とした粒度

---

## MVP：基本機能の動作

**目的**：イベント検知から音声・テキスト出力までの基本フローを動かす

### M1. 開発環境構築
- Dalamud SDK のセットアップ
- SamplePluginを土台にプロジェクト初期化
- ビルド・ホットリロード動作確認

### M2. プラグイン基盤
- プラグインのライフサイクル（Initialize / Dispose）
- 設定ファイル（config.json）の読み書き
- スラッシュコマンド基盤（`/myplugin`）
- ImGui ウィンドウの基本表示

### M3. イベントキャプチャ
- キャスト開始/完了 検知
- ステータス付与/消失 検知
- アクション発動 検知
- HP変化 検知
- 戦闘開始/終了 検知
- マップ移動 検知
- 観測イベントを内部イベントバスに発行

### M4. ロガー（基本）
- イベントをファイル書き出し（JSON Lines）
- コンテンツ別フォルダ分け
- 戦闘単位での分割
- スラッシュコマンドでの記録ON/OFF

### M5. トリガー定義の読み込み
- JSONファイル読み込み
- スキーマ検証
- 起動時に全コンテンツのトリガー定義をロード
- ファイル変更検知での自動リロード

### M6. トリガーエンジン（基本）
- 検知タイプ別のマッチング
  - cast_start / cast_complete
  - status_gain / status_lose
  - action_used
  - hp_change
  - combat_start / combat_end
  - zone_change
- 対象識別（self / other / party / ロール / マーカー）
- 単純なマッチ条件のみ（複合条件は後）

### M7. アクションディスパッチャ（基本出力）
- TTS読み上げ（SAPI5）
- WAVファイル再生
- 画面中央への大型テキスト表示
- タイマーバー表示
- チャットエコー
- delay対応

### M8. 設定画面（基本）
- コンテンツ一覧表示
- 集計ビュー（観測イベント一覧テーブル）
- 簡易トリガー編集（マッチ条件 + 1〜2個のアクション）
- 設定済み/未設定/無視済み の3状態管理
- トリガー保存（コンテンツ単位のJSONファイル）

### M9. 音量・音声デバイス管理
- マスター音量
- カテゴリ別音量（TTS / WAV）
- 出力デバイス選択

### M10. バックアップ機構
- トリガー定義変更時の自動バックアップ
- バックアップからの復元UI

**MVP完了条件**：極エヌオー討滅戦で「キャスト検知 → 音声読み上げ + テキスト表示 + タイマー」のサイクルが動く

---

## フェーズ2：絶コンテンツ実用機能

**目的**：絶妖星乱舞の攻略実用に耐える機能群

### F1. タイムラインビュー
- 横軸時間 + 複数レーンの表示
- 観測されたイベントの色分け表示
- イベントクリックで編集パネル
- 複数ログのマージ表示
- 観測回数の表示

### F2. 複合条件
- AND/OR 条件
- 条件のネスト
- デバフ秒数による分岐（duration_range）
- スタック数による分岐
- HP閾値の上下限指定

### F3. ライブHUD（タイムライン）
- 戦闘中の画面オーバーレイ
- 水平バー型タイムライン
- 位置・サイズのドラッグ調整
- 表示モード切替（設定済みのみ / 全イベント）
- sync機構（イベント観測による時刻補正）
- フェーズスキップ判定

### F4. 安置計算プリセット（基本）
- fixed
- boss_relative
- marker_relative
- arena_center_relative
- inverse_of_telegraph
- party_member_relative

### F5. 安置計算プリセット（オブジェクト検知）
- find_actor_with_status
- find_actor_without_status
- find_actor_not_casting
- find_actor_by_distance

### F6. 安置計算プリセット（関係性）
- midpoint
- line_perpendicular
- between_actors

### F7. 安置計算プリセット（高度）
- telegraph_gap（ジオメトリ計算）
  - 円範囲の対応
  - 扇範囲の対応
  - 直線範囲の対応
  - ドーナツ範囲の対応
  - 矩形範囲の対応
- intersection（複合制約）

### F8. 位置情報出力
- direction_call（方角コール、各format対応）
- screen_arrow（画面上の方向矢印）
- field_marker（フィールド上のワールド座標マーカー）
- proximity_feedback（安置内/外フィードバック）
- 距離表示

### F9. オブジェクト監視
- ObjectTable のポーリング
- 出現/消失検知（object_appear / object_disappear）
- 座標・向きの取得
- 効率化（差分検知、変更があったときだけ通知）

### F10. プロファイル機能
- 複数プロファイルの作成
- プロファイル切替
- プロファイル別の有効トリガー管理
- ジョブ自動切替（着替え時に対応プロファイル選択）

### F11. インポート/エクスポート
- 単一コンテンツのトリガー定義をJSON出力
- インポート時のコンフリクト処理（上書き/マージ/スキップ）
- フォーマットバージョン管理

### F12. 設定画面の拡張
- タイムラインビュー / 集計ビュー の切替
- 詳細編集パネル（複合条件ビルダー）
- アクション複数追加・並べ替え
- 安置計算プリセットのGUI設定
- プレビュー機能（テスト発動）

**フェーズ2完了条件**：絶コンテンツの主要ギミック（位置指示・複合条件・タイムライン）に対応可能

---

## フェーズ3：高度な状態管理

**目的**：複雑な分岐ギミックや「N回目で違う処理」への対応

### P1. 状態変数
- 変数の定義・参照
- set / increment / decrement / reset 操作
- 戦闘終了時の自動リセット
- 永続化オプション

### P2. 条件分岐の拡張
- variable参照による条件分岐
- 複合条件内での変数評価
- 動的な値の比較（greater_than / less_than など）

### P3. 連鎖トリガー
- chain_trigger アクション
- 別トリガーの強制発動
- 条件付き連鎖

### P4. 履歴ベース安置（stored_position）
- 位置の保存・参照
- 「前回の処理位置」の記録
- 「前回と違う位置」の判定

### P5. カスタムスクリプト
- C# コードの外部読み込み
- Claude Code で生成したロジックを動的にロード
- 安全なサンドボックス（必要に応じて）

### P6. 高度なUI
- 複数オーバーレイの同時管理
- 条件付き表示
- アニメーション

---

## フェーズ外（将来検討）

以下は仕様外として残す可能性がある項目：

- Webベースの設定画面（外部ブラウザでの編集）
- クラウド同期（複数PCで設定共有）
- コミュニティトリガー共有プラットフォーム
- 録画ログの可視化・統計（DPS分析的なやつ）
- 多言語対応（英語版・グローバル版クライアント）

---

## マイルストーン

| 時期 | 目標 |
|------|------|
| 即着手 | M1〜M3 完了（イベントキャプチャまで） |
| +1週 | M4〜M7 完了（基本トリガー動作） |
| +2週 | M8〜M10 完了（MVP完了、極エヌオーで動作確認） |
| +3週 | F1〜F3 完了（タイムライン・複合条件） |
| +4週 | F4〜F8 完了（安置計算群） |
| +5週 | F9〜F12 完了（フェーズ2完了） |
| 6月2日 | 絶妖星乱舞 実装、ツール投入開始 |
| 6月以降 | 攻略中にP1〜P6を順次追加 |

実装ペースは目安。Claude Code 活用前提なので、検証と修正のサイクルを短く回す。

---

## リスクと対策

### Dalamud APIの仕様変更
パッチアップデート時にプラグインが動作しなくなる可能性。
**対策**：API変更点を最小化する設計、変更があった部分の影響範囲を限定。

### 絶妖星乱舞のギミック実態が事前に分からない
実装後に判明するため、想定外のギミックパターンが出る可能性。
**対策**：プリセットを汎用的に設計、カスタムスクリプト（フェーズ3）で穴を埋める拡張ポイントを残す。

### 規約変更によるツール利用制限
Square Enix の方針変更でDalamudプラグイン全般が利用制限される可能性。
**対策**：機能を「情報表示・読み上げ」に限定して規約違反リスクを最小化。

### 開発工数の超過
仕様が肥大化しているため、MVPだけで時間切れの可能性。
**対策**：フェーズ分割を厳格に運用、MVPで動くものを早期に確保してから機能追加。

---

## 既知の技術的負債（絶妖星乱舞後に対処）

リリース可能な状態だが、長期メンテナンス性の観点から将来整理したい項目。
それぞれ単体では実害がないため、**MVP / フェーズ2 のスコープ外**として記録。

### TD-1：`Recording` 名前空間 ↔ `Triggers` 名前空間の双方向依存

- `RecordingController` が `TriggerStore` を参照（auto_record 設定読み）
- `Triggers/StrategyDraftGenerator` 等 10+ ファイルが `Recording/AggregatedEvents` 等を参照

論理的な双方向依存が成立している。コンパイル循環ではないが、変更影響範囲が読みにくい。
**対処案**：`TriggerStore` から `auto_record` 設定値だけ取り出して `RecordingController` のコンストラクタに渡す DI に変更。所要 4-8 時間。

### TD-2：`AutoSafeCallPlanner` の `static class` + `static Dictionary`

`coding-rules.md` の「サービスロケータパターン禁止」のグレーゾーン。
**現状の対処**：`Initialize()` メソッドで null セット禁止 + 二重初期化警告ログ済み。
**完全 DI 化案**：`IAutoSafeCallPlanner` interface 化 + 30+ callsite を instance 経由に変更。所要 2-3 時間。

### TD-3：`StatusCapture.IsPlayerStatus` のユニットテスト不足

`IObjectTable.SearchById` 経由の動的判定パスは Dalamud 依存のため Program.cs（コンソールテストランナー）からテストできない。
**対処案**：`ILuminaPcDetector` インターフェース化と FakeObjectTable 導入。所要 3-5 時間。
**現状の代替**：イベント値オブジェクトレベルの契約テスト（`StatusGainedEvent_BossDebuffOnPc_IsNotPlayer` 等）と手動の Dalamud 動作確認。
